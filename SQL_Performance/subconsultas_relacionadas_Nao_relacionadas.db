/*
   As subsconsultas permitem utilizar uma coluna qualificada externa (Ou seja, o Select fora da consulta como meio de uso para o select interno),
   de acordo com o autor, este tipo de consulta gera um maior tempo de processo, assim sendo não indicado o seu uso. Porém acredito que seja interessante falar sobre. 
	
*/


USE Fluent_SQL;

SELECT c.customer_id, c.gender, c.age_group, c.region (SELECT COUNT(*) FROM ORDERS AS c
						       WHERE p.product_id = c.customer_id) AS Total_Products
FROM Customer_Sentiment
GROUP BY c.customer_id
LIMIT 20;



# O mesmo pode e deverá ser feito com o uso de 	JOIN 
